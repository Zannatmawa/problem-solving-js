const vowels = "programming";
let count = 0;
for (var i = 0; i < vowels.length; i++) {
    console.log(i);
    // if (i == 'a' || i == 'e' || i == 'i' || i == 'o' || i == 'u') {
    //     count++
    //     console.log('a');
    // }

    for (var j = 0; j < vowels[i]; j++) {
        console.log(j)
    }
}